# Task: fix the failing tests in this key-value store

A small in-memory key-value store with TTL and a command parser. Three tests
fail. Fix the bugs in src/ so all tests pass. Do NOT change the tests.
Run the tests with: python3 tests/test_kv.py
