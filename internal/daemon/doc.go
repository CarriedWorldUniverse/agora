// Package daemon — the long-lived daemon wiring the seams together; conformance target.
//
// Build unit: U18 (docs/spec/agora-spec-build.md §1).
// Spec: docs/spec/agora-spec-io.md §0a.
//
// Skeleton only: this package is scaffolded at U1 and implemented by
// U18. It builds against the compiled seams in the contracts package.
package daemon
