// Package memory — file-based identity-scoped memory + the memory.* tool family.
//
// Build unit: U13 (docs/spec/agora-spec-build.md §1).
// Spec: docs/spec/agora-spec-memory.md.
//
// Skeleton only: this package is scaffolded at U1 and implemented by
// U13. It builds against the compiled seams in the contracts package.
package memory
