// Package mcp — MCP manager + native tool families + tool_search + the fs-watcher runtime.
//
// Build unit: U8 (docs/spec/agora-spec-build.md §1).
// Spec: docs/spec/agora-spec-mcp.md.
//
// Skeleton only: this package is scaffolded at U1 and implemented by
// U8. It builds against the compiled seams in the contracts package.
package mcp
