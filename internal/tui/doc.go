// Package tui — the lean bubbletea TUI (new; coexists with internal/ui until parity).
//
// Build unit: U15 (docs/spec/agora-spec-build.md §1).
// Spec: docs/spec/agora-spec-tui.md.
//
// Skeleton only: this package is scaffolded at U1 and implemented by
// U15. It builds against the compiled seams in the contracts package.
package tui
